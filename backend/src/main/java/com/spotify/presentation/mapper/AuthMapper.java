package com.spotify.presentation.mapper;

import org.springframework.stereotype.Component;

/**
 * Mapper for Presentation Layer to convert Web Input Models 
 * to Application layer DTOs if they differ.
 */
@Component
public class AuthMapper {
    // Currently using the App DTOs directly in the controller for simplicity.
    // Methods to map Web requests -> Application DTOs would go here.
}
