server:
  port: 8084

spring:
  application:
    name: spotify-clone-playlist-service
  datasource:
    url: jdbc:postgresql://localhost:5433/playlist_db
    username: postgres
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
  flyway:
    enabled: true
    baseline-on-migrate: true
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  jackson:
    default-property-inclusion: NON_NULL

services:
  user-service:
    # Cross-service broker for resolving owner display names. Env-overridable so
    # localhost works for the smoke harness and container-name for docker-compose.
    base-url: ${USER_SERVICE_BASE_URL:http://localhost:8088}

# ===== OpenAPI / Swagger UI =====
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method